// importing junit libraries
import static org.junit.Assert.*;
import org.junit.*;

public class MarkdownParseTest {
    @Test //Flag for indicating its a Junit test
    public void addition() {
        // Passes if 2 equals 1 + 1, fails if they are not equal
        assertEquals(2, 1 + 1);
    }
}
